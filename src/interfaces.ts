export interface mainData{
  topLine: string,
  name: string,
  language: string
  description: string,
  mainFocus: string
  status: string,
  webpage: string,
  gitHub: string,
  img: string,
  alt: string
}

export interface languageFilter {
  js?: boolean,
  ts?: boolean
}